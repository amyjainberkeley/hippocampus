# Test fixture — CSO-ratified deny entries (immutable in production).
# Tests assert these cannot be removed via the editor store.

[[entries]]
type = "bundleId"
value = "com.banking.secretapp"

[[entries]]
type = "windowTitle"
value = "^Password Manager.*"

[[entries]]
type = "urlPattern"
value = "https://internal\\.corp\\.example\\.com/.*"
