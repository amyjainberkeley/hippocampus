# Python dependencies for ML model conversion scripts.
# NOT used at runtime or in CI — operator-only, manual conversion.
# See scripts/convert_embedder.py and scripts/convert_brief_model.py.
#
# torch<2.6 is required: coremltools 8.x op registry expects torch <=2.5
# op signatures. Newer torch versions change traced op names/signatures,
# causing "unordered_map::at: key not found" during ct.convert.
coremltools>=8.0
torch>=2.4,<2.6
numpy<2.0
transformers>=4.40
optimum>=1.23
onnx>=1.17
onnxruntime>=1.20
