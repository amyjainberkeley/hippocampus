# September 7: Memory Quality And Website

The owner requests clearer positioning, a brain identity, stronger OCR/search,
honest capture coverage and useful summaries/insights. Product truth remains
`docs/STATUS.md`; this plan does not qualify an installer or claim enterprise
certification.

## Execution Order

1. Redesign `website/` around private personal memory for humans and agents.
   Replace the old mark and screenshot hero. Explain local processing, source
   inspection, focused-window scope and sharing boundaries. Preserve release
   gates, accessibility, existing audience and the hosting identity. Validate,
   publish the site, and push its source checkpoint to the canonical repository.
2. Reproduce OCR omissions with synthetic native-resolution text. Compare old
   and new recognition on the same corpus before claiming improvement. Preserve
   raw identifiers, admitted-region boundaries, timeout quarantine and privacy
   scanning. More pixels or passes need measured resource justification.
3. Reproduce search failures independently of recognition: searchable text must
   survive punctuation, common query forms and bounded retrieval without SQL/FTS
   injection or silently weakened evidence filters. Run regression tests.
4. Improve extractive summaries using source-preserving tests. Do not turn OCR
   observations into verified accomplishments. Keep source navigation, export
   limits, date boundaries and explicit uncertainty.
5. Qualify integrated native code before rebuilding and replacing the installed
   signed artifact. Record source-tested, installed and live-proven separately.

## Product Boundaries To Build Next

- Focused-window capture is the current privacy boundary. Broader co-visible
  capture must classify every admitted window, exclude private/ambiguous sources,
  survive overlap/focus races and have an explicit user control. Reading unopened
  tabs requires a separate source, not a screenshot claim.
- Useful work insights require persisted active/idle samples, explicit unknown
  gaps, sleep/lock/pause handling and retention/deletion. Sample timestamps do
  not establish time worked. Application names do not establish productivity.
- Planned-versus-completed tasks require reviewable task records, stable source
  citations, change history and user confirmation. A visible checkbox or chat
  promise alone cannot authorize an action or prove an accomplishment.
- Deeper understanding must measure held-out source attribution, contradiction,
  temporal changes and abstention, not just fluent summaries. No autonomous
  follow-through until separate action permissions and audit history exist.

## Definition Of This Checkpoint

The website is deployed to its existing audience; source is pushed to the
existing draft PR. OCR/search changes have observed failing regressions and
passing targeted checks. The results name remaining gaps and do not conflate
new source with the installed app or a public downloadable release.
