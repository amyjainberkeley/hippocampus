# Per ADR-0005. MSRV remains `1.83` in workspace package metadata, while the
# repository build and release toolchain is pinned independently. Update this
# exact version only in a reviewed toolchain bump after fmt, Clippy, and tests.

[toolchain]
channel    = "1.96.0"
components = ["rustfmt", "clippy"]
profile    = "minimal"
